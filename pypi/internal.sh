rm -rf /root/.config/pip/pip.conf
pip config set global.index-url https://{your domain}/simple

# If not using https, please uncomment this line to set trusted host
#pip config set global.trusted-host {your domain}
